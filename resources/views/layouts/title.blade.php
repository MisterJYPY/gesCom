

@if($title=='listeclient')
    Liste des Clients
    @endif

@if($title=='ajoutereclamation')
Liste des Reclamations
@endif


@if($title=='rdv')
    Liste des Rendez-Vous
@endif



@if($title=='listevisite')
    Liste des Visites
@endif

@if($title=='achive.reclamation')
        Liste des Reclamations Annulées
@endif

@if($title=='achive.rdv')
    Liste des RDV Archivés
@endif

@if($title=='userlog')
    Liste des commerciaux
@endif
@if($title=='listReclame')
    Liste des Reclamations
@endif

@if($title=='listeRapport')
    Liste des rapports
@endif


@if($title=='acceuiAdmin')
    Liste des commerciaux
@endif


@if($title=='listeAdm')
    Liste des administrateurs
@endif


@if($title=='listClient')
    Liste des Clients
@endif

@if($title=='listRdv')
    Liste des Rendez-Vous
@endif

@if($title=='listReclamation')
    Liste des Reclamations
@endif

@if($title=='client.Commercial')
    Liste des Clients
@endif